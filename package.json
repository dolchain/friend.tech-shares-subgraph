{
  "name": "friend-tech-shares",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ friend-tech-shares",
    "create-local": "graph create --node http://localhost:8020/ friend-tech-shares",
    "remove-local": "graph remove --node http://localhost:8020/ friend-tech-shares",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 friend-tech-shares",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.53.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
